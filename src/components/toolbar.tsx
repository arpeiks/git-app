import { styled } from "@utils/styled";

const Toolbar = styled("div", {
  width: "100%",
  display: "inherit",
  padding: "1rem 2rem",
  alignItems: "inherit",
  justifyContent: "inherit",
});

export default Toolbar;
