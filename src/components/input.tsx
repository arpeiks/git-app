import { styled } from "@utils/styled";

const Input = styled("input", {
  margin: 0,
  outline: 0,
  width: "100%",
  padding: "1rem",
  fontSize: "1rem",
  borderRadius: "4px",
  border: "1px solid #e3e3e3",
});

export default Input;
